import { UniqueRoleValidatorDirective } from './unique-role-validator.directive';

describe('UniqueRoleValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueRoleValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
