import { RoutingReprocessData } from './routing-reprocess-data';

describe('RoutingReprocessData', () => {
  it('should create an instance', () => {
    expect(new RoutingReprocessData()).toBeTruthy();
  });
});
