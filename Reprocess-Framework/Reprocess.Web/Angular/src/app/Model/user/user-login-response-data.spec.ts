import { UserLoginResponseData } from './user-login-response-data';

describe('UserLoginResponseData', () => {
  it('should create an instance', () => {
    expect(new UserLoginResponseData()).toBeTruthy();
  });
});
