import { MtReprocessDataGrid } from './mt-reprocess-data-grid';

describe('MtReprocessDataGrid', () => {
  it('should create an instance', () => {
    expect(new MtReprocessDataGrid()).toBeTruthy();
  });
});
